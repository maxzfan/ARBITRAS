# Stream provenance — out/casevac.jsonl (Track F mission stream)

Generated by `python -m backend.missions --mission casevac`. Same field
classification as docs/stream_provenance.md (features measured/injected,
position solved by differential WLS, `_truth` the clean fix, credential
SCRIPTED, `_attack` the injector's truth log). Differences for this stream:

- **Window:** 300 epochs, 2026-08-20T12:00:00Z → 2026-08-20T14:29:30Z: 60 clean lead-in,
  60 attack, 180 post. Scored after a
  45-epoch warm-up (dropped) so the trailing baselines are real.
- **Attack:** carry_off (position domain): 60 attack epochs {'WALK': 59, 'CAPTURE': 1}, 6 SV at peak, max commanded displacement 1760.0 m on bearing 135 deg, max code-carrier divergence 0.00 m. Scenario `carry_off`, power 2.0 dB,
  walk 1.0 m/s (position), step 0.0 m,
  common bias 0.0 m, carrier_rate_error 0.0 m/s,
  bearing 135.0. Carrier-coherent along-track walk toward the CCP; Track E terrain channel on with a SIMULATED sensor (diag 0.85, stated); the route-constrained terrain fix is added by augment() (TRACK_F_CASEVAC.md).
- **Exclusion rule:** `k1` — per-SV post-fit residual |z| ≥ calibrated saturation (demo.py).
- **Correction gate alert limit:** 15.0 m.
- **Credential schedule:** `valid` — VALID 300 · PENDING 0 · EXPIRED 0 epochs (scripted; T_int 60 × d 2).
- **Terrain channel (Track E):** ON — SIMULATED sensor, confusion diagonal 0.85 (stated, swept parameter), window 1; signed OSM pre-map; calibrated on this slice's clean fixes.
- **Solver sanity on the slice:** clean fix vs survey horizontal p50 0.87 m, max 1.56 m, unsolvable 0.

## Measured on this stream

| quantity | value |
|---|---|
| lead-in max displacement | 0.0000 m |
| first attack epoch with displacement > 1 m | 61 (slice index; onset 60) |
| peak attack displacement | 632.608 m |
| displacement at attack end | 632.608 m |
| post-attack max displacement | 0.0 m |
| confidence lead-in min | 0.7661 |
| confidence attack p50 / p95 | 0.160 / 0.319 |
| excluded satellites during attack (median / max) | 27.0 / 30 |
| correction_ok during attack / after | 2/60 / 150/180 |

Attack epochs by stage: {'WALK': 59, 'CAPTURE': 1}.
State timeline: `python -m console.replay out/casevac.jsonl`.
