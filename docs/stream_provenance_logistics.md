# Stream provenance — out/logistics.jsonl (Track F mission stream)

Generated by `python -m backend.missions --mission logistics`. Same field
classification as docs/stream_provenance.md (features measured/injected,
position solved by differential WLS, `_truth` the clean fix, credential
SCRIPTED, `_attack` the injector's truth log). Differences for this stream:

- **Window:** 510 epochs, 2026-08-20T12:00:00Z → 2026-08-20T16:14:30Z: 60 clean lead-in,
  90 attack, 360 post. Scored after a
  45-epoch warm-up (dropped) so the trailing baselines are real.
- **Attack:** carry_off (position domain): 90 attack epochs {'WALK': 89, 'CAPTURE': 1}, 6 SV at peak, max commanded displacement 532.0 m on bearing 90 deg, max code-carrier divergence 53.20 m. Scenario `carry_off`, power 2.0 dB,
  walk 0.2 m/s (position), step 0.0 m,
  common bias 0.0 m, carrier_rate_error 0.02 m/s,
  bearing 90.0. 0.2 m/s walk (venue-tuned inside §7's 'tune at the venue') so the corridor crossing and the corrected-fix window are legible at 30 s epochs; the 1 m/s reference is the original demo (TRACK_F_LOGISTICS.md).
- **Exclusion rule:** `k1` — per-SV post-fit residual |z| ≥ calibrated saturation (demo.py).
- **Correction gate alert limit:** 15.0 m.
- **Credential schedule:** `lapse` — VALID 270 · PENDING 120 · EXPIRED 120 epochs (scripted; T_int 60 × d 2).
- **Terrain channel (Track E):** off.
- **Solver sanity on the slice:** clean fix vs survey horizontal p50 0.81 m, max 1.56 m, unsolvable 0.

## Measured on this stream

| quantity | value |
|---|---|
| lead-in max displacement | 0.0000 m |
| first attack epoch with displacement > 1 m | 61 (slice index; onset 60) |
| peak attack displacement | 205.409 m |
| displacement at attack end | 205.409 m |
| post-attack max displacement | 0.0 m |
| confidence lead-in min | 0.8224 |
| confidence attack p50 / p95 | 0.295 / 0.630 |
| excluded satellites during attack (median / max) | 22.0 / 25 |
| correction_ok during attack / after | 6/90 / 330/360 |

Attack epochs by stage: {'WALK': 89, 'CAPTURE': 1}.
State timeline: `python -m console.replay out/logistics.jsonl`.
