# Stream provenance — out/combat.jsonl (Track F mission stream)

Generated by `python -m backend.missions --mission combat`. Same field
classification as docs/stream_provenance.md (features measured/injected,
position solved by differential WLS, `_truth` the clean fix, credential
SCRIPTED, `_attack` the injector's truth log). Differences for this stream:

- **Window:** 510 epochs, 2026-08-20T12:00:00Z → 2026-08-20T16:14:30Z: 60 clean lead-in,
  20 attack, 430 post. Scored after a
  45-epoch warm-up (dropped) so the trailing baselines are real.
- **Attack:** simplistic_position (position domain): 20 attack epochs {'WALK': 19, 'CAPTURE': 1}, 12 SV at peak, max commanded displacement 250.0 m on bearing 90 deg, max code-carrier divergence 7.84 m. Scenario `simplistic_position`, power 15.0 dB,
  walk 0.0 m/s (position), step 250.0 m,
  common bias 0.0 m, carrier_rate_error 0.014 m/s,
  bearing 90.0. Crude 15 dB step on all GPS, 250 m commanded (stated); 20 attack epochs so the baselines recover within the slice (TRACK_F_COMBAT.md).
- **Exclusion rule:** `constellation_first` — a whole-constellation disagreement on the cross-constellation clock/position channels (at the feature saturation) excludes exactly that constellation; otherwise the k=1 per-SV residual rule.
- **Correction gate alert limit:** 15.0 m.
- **Credential schedule:** `renewal` — VALID 390 · PENDING 120 · EXPIRED 0 epochs (scripted; T_int 60 × d 2).
- **Terrain channel (Track E):** off.
- **Solver sanity on the slice:** clean fix vs survey horizontal p50 0.81 m, max 1.56 m, unsolvable 0.

## Measured on this stream

| quantity | value |
|---|---|
| lead-in max displacement | 0.0000 m |
| first attack epoch with displacement > 1 m | 60 (slice index; onset 60) |
| peak attack displacement | 102.971 m |
| displacement at attack end | 102.971 m |
| post-attack max displacement | 0.0 m |
| confidence lead-in min | 0.8224 |
| confidence attack p50 / p95 | 0.094 / 0.175 |
| excluded satellites during attack (median / max) | 27.5 / 28 |
| correction_ok during attack / after | 0/20 / 400/430 |

Attack epochs by stage: {'WALK': 19, 'CAPTURE': 1}.
State timeline: `python -m console.replay out/combat.jsonl`.
